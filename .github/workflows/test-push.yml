name: Test Push Trigger

on:
  push:   # 任何 push 都會觸發

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: Print info
        run: echo "🚀 Push event triggered on ${{ github.ref }} by ${{ github.actor }}"
