{
  "scripts": {
    "start": "docusaurus start",
    "build": "docusaurus build",
    "serve": "docusaurus serve",

    "build:gh": "docusaurus build",                 // 給 GitHub Pages：BASE_URL 走預設 /openmark/
    "build:vercel": "cross-env BASE_URL=/ docusaurus build"  // 給 Vercel：把 BASE_URL 設成 /
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
