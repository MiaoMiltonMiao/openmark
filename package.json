{
  "scripts": {
    "start": "docusaurus start",
    "build": "docusaurus build",
    "serve": "docusaurus serve",
    "build:gh": "cross-env BASE_URL=/openmark/ docusaurus build",
    "build:vercel": "cross-env BASE_URL=/ docusaurus build",
    "lint:docs": "node scripts/lint-doc-links.js"
  },
  "dependencies": {
    "@docusaurus/core": "^3.8.1",
    "@docusaurus/preset-classic": "^3.8.1",
    "prism-react-renderer": "^2.4.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "fast-glob": "^3.3.3"
  },
  "engines": {
    "node": ">=20 <21"
  }
}
